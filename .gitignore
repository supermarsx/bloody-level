# === Frontend / Node ===
node_modules/
.svelte-kit/
build/
site/
dist/
.vite/
.cache/
.turbo/
*.tsbuildinfo

# === Rust / Tauri ===
src-tauri/target/
src-tauri/gen/
# Cargo.lock IS committed (this is a binary app — reproducible builds)

# Tauri sidecar binaries (downloaded/built locally; keep the dir via .keep)
src-tauri/binaries/*
!src-tauri/binaries/.keep

# === App data (never commit) ===
data/
data test/
*.db
*.db-shm
*.db-wal
keystore.json
keystore.json.tmp

# === Models (large binaries; downloaded on first run) ===
models/
*.gguf
*.bin
*.onnx
*.safetensors

# === Local-only env ===
.env
.env.*
!.env.example

# === Logs ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === OS / IDE ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
/src-tauri/target-native-check
